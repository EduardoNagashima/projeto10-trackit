import { PageFooter } from "./style";

export default function Footer() {
    return (
        <PageFooter>
            <span>Hábitos</span>
            <div>
                Hoje
            </div>
            <span>Histórico</span>
        </PageFooter>
    );
}